# Secrets & local config
.env

# Local database (never commit synced Strava data)
.database/

# Application logs
.logs/

# Spec Kit / AI dev tooling — kept local, not versioned (CLAUDE.md stays tracked)
.specify/
.claude/

# Python / uv
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
build/
dist/
*.egg-info/
